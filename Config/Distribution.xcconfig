#include "Application.xcconfig"

// External distribution remains inactive; local signing uses LocalSigning.xcconfig.
// Reconfirm before enabling this public identifier or its submission workflow.
LENS_DISTRIBUTION_BUNDLE_IDENTIFIER =
PRODUCT_BUNDLE_IDENTIFIER = $(LENS_DISTRIBUTION_BUNDLE_IDENTIFIER)
CODE_SIGN_IDENTITY = Developer ID Application
CODE_SIGN_INJECT_BASE_ENTITLEMENTS = NO
// Supply DEVELOPMENT_TEAM from the signing environment; never store credentials here.
